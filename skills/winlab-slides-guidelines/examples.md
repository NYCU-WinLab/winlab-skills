This file lists slide contents generated by Claude, and why is it good or bad.

---

Content:

```
- Device support implies a hardware floor
  - MacStories: at least 12 GB unified memory is required
  - MacRumors: full device list, 8 GB devices are excluded
```

Good since these two items elaborates why device support implies a hardware floor, while all the lines are in an adequate length

---

Content:

```
- Multi-Head Attention measures how much each token attends to the others
- Add and Norm wraps each part (residual addition plus layer normalization)
- Feed-Forward Network does per-token reasoning after relations are known
- Encoder attention sees the whole input, decoder attention is causal and sees only earlier tokens
- A FFN is two linear layers with an activation in between
- IFP prunes the FFN
<An image showing the structure of the encoder and the decoder of Transformer>
```

Bad because:
- Higher level concepts should be explained first then details in it. So the description regarding to encoder and decoder (the 4th item) should be listed at first, and then multi-head attention, add and norm, and FFN since they are components in an encoder and a decoder. Then the 5th item describing FFN's structure should be listed.
- "IFP prunes the FFN" shouldn't be listed here. Although IFP (a LLM pruning method) indeed prunes the FFN, this slide is talking about the structure in Transformer.

---

Title: Static pruning: unstructured versus structured

Good. Although colons should be less used in slides, we should use less words in a title. Plus, "unstructured" and "structured" are methods of static pruning, so this title is explaining sub-category of static pruning.

---

Title: Mixture-of-Experts: caveats and on-device cost

Content:

```
- It needs a load-balancing loss so tokens do not all pile onto one expert
- Too strong gives random routing, too weak causes expert collapse, so it is hard to tune
- Examples: Switch Transformer, Mixtral 8x7B, DeepSeek-V3
- On device, swapping experts per token makes decode slow (28.43 s)
```

Good:
- Clearly states out the contents included in this slide

Bad:
- "It" in the first point should be replaced by "MoE" since it's clearly states the subject and the length is nearly the same
- The first two items are both saying the caveats that MoE needs a load-balancing loss brings, so these two lines can be reorganized as title item and sub-items below the title item
- Example models using MoE are listed right after the problem the load-balancing loss brings, making the audience thinks those models are examples of the problem the load-balancing loss brings
- The background of the slowed down time 28.43s should shown together

Side note: the description with respect to MoE itself has already be shown just in the previous slide.

---

Title: Three questions about the results

Content:

```
- Is per-input pruning accurate compared with a same-size dense model?
- Do different tasks really pick different sub-networks?
- Does running the predictor first add noticeable time?
```

Good. This is the slide placed before all stuffs about the results of a paper. This slide makes the audience focuses on the key points the following section includes, before looking into any details. The title "three questions" and the corresponding 3 bullet points matches, the all the questions are asking in different factors.
